apiVersion: 1

providers:
  - name: Ghost
    orgId: 1
    folder: Ghost
    folderUid: ghost
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
