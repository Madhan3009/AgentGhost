services:
  - type: web
    name: agentghost
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: bash start.sh
    envVars:
      - key: PYTHONPATH
        value: .
      - key: IS_DEVELOPMENT
        value: false
